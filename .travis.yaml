dist: xenial   # required for Python >= 3.7
language: python
rvm:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: tools_tests.py
